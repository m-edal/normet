% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_prepare_data.r
\name{nm_prepare_data}
\alias{nm_prepare_data}
\title{Prepare Data for Model Training}
\usage{
nm_prepare_data(
  df,
  value,
  feature_names,
  na_rm = TRUE,
  split_method = "random",
  fraction = 0.75,
  seed = 7654321
)
}
\arguments{
\item{df}{Input data frame.}

\item{value}{The target variable name as a string.}

\item{feature_names}{The names of the features used for training.}

\item{na_rm}{Logical indicating whether to remove rows with missing values.}

\item{split_method}{The method for splitting data into training and testing sets. Default is 'random'.}

\item{fraction}{The proportion of the data to be used for training. Default is 0.75.}

\item{seed}{Random seed for reproducibility. Default is 7654321.}

\item{replace}{Logical indicating whether to replace existing date variables.}
}
\value{
Prepared data frame.
}
\description{
\code{nm_prepare_data} performs a series of data preparation steps including processing,
checking, imputing, adding date variables, and splitting into sets.
}
\examples{
\dontrun{
library(dplyr)
df <- data.frame(
  feature1 = rnorm(100),
  feature2 = rnorm(100),
  target = rnorm(100)
)
prepared_df <- nm_prepare_data(df, value = "target", feature_names = c("feature1", "feature2"))
}
}
