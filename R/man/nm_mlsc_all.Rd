% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_scm.r
\name{nm_mlsc_all}
\alias{nm_mlsc_all}
\title{Apply synthetic control with ML models to all treatment targets}
\usage{
nm_mlsc_all(
  df,
  poll_col,
  code_col,
  control_pool,
  cutoff_date,
  model_config,
  training_split = 0.75,
  n_cores = NULL,
  verbose = FALSE
)
}
\arguments{
\item{df}{Data frame containing the input data.}

\item{poll_col}{The name of the column containing the pollutant data.}

\item{code_col}{The name of the column containing the unit codes.}

\item{control_pool}{A vector of codes representing the control units.}

\item{cutoff_date}{The date used to split the data into pre-treatment and post-treatment periods.}

\item{model_config}{A list containing configuration parameters for model training.}

\item{training_split}{The proportion of the data to be used for training. Default is 0.75.}

\item{n_cores}{Number of CPU cores to use for parallel processing. Default is system's total minus one.}

\item{verbose}{Should the function print progress messages? Default is FALSE.}
}
\value{
A list containing the synthetic control data for all treatment targets, model statistics, and the trained models.
}
\description{
\code{nm_mlsc_all} applies machine learning models to estimate the synthetic control for all treatment targets in parallel.
}
\examples{
\dontrun{
library(dplyr)
df <- data.frame(
  date = Sys.time() + seq(1, 100, by = 1),
  pollutant = rnorm(100),
  unit_code = rep(c("A", "B", "C", "D"), each = 25)
)
result <- nm_mlsc_all(df, poll_col = "pollutant",
                      code_col = "unit_code", control_pool = c("B", "C", "D"),
                      cutoff_date = "2020-01-01", model_config = list(max_models = 5, time_budget = 600))
}
}
